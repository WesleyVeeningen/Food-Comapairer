<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/tailwind.css">
    <style>
        /* Add your custom styles here */
        .bg-stone-700 {
            background-color: #2b3148;
        }

        .text-yellow-500 {
            color: #fbbf24;
        }

        .form-group label {
            color: #fff;
        }

        .form-group input[type="text"],
        .form-group input[type="password"] {
            background-color: #fff;
            color: #000;
            border-radius: 0.25rem;
            padding: 0.5rem;
        }

        .form-group input[type="text"]:focus,
        .form-group input[type="password"]:focus {
            outline: none;
            box-shadow: 0 0 0 2px #fbbf24;
        }

        .btn-login {
            background-color: #fbbf24;
            color: #fff;
            border: none;
            border-radius: 0.25rem;
            padding: 0.75rem 1rem;
            cursor: pointer;
        }

        .btn-login:hover {
            background-color: #f59e0b;
        }

        .password-toggle {
            position: relative;
        }


        .password-toggle .toggle-btn {
            position: absolute;
            top: 75%;
            right: 0.75rem;
            transform: translateY(-50%);
            cursor: pointer;
        }
    </style>
</head>
<body class="bg-stone-700">
    <div class="fixed bg-stone-700">
        <p class="text-white text-3xl m-2">Food<span class="text-yellow-500">Compairer</span></p>
    </div>

    <div class="flex  justify-center items-center h-screen bg-stone-700">
        <div class="bg-gray-200 p-8 rounded-lg">
            <h1 class="text-2xl font-bold mb-4">Please verify your account to use the website</h1>
            <img src="/images/mailbox.svg" alt="Verification Image" class="mb-4 w-[10rem]">
            <!-- Additional content here -->
        </div>
    </div>
</body>
</html>
